# Vision

Build a local-first visual operating system for files.

Core principles:
- Filesystem is the source of truth.
- No importing or duplicating data.
- Local-first.
- Multiple synchronized views (Canvas, Graph, Table, Explorer).
- Metadata is separate from user files.
- Open architecture with plugins.
